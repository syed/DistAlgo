from runtime.sim import DistProcess

global DistProcess
global TIMEOUT
TIMEOUT = 3
dist_source("leader.dis")

def config():
    n = 5
    ps = createprocs(P, n)
    config_sim_total_units(100)
    for i in range(0, n):
        AllProcesses[i].setup((i+1 if i < (n-1) else 0))
    config_trace(True)