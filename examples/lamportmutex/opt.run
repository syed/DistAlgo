from runtime.sim import DistProcess

global DistProcess
global TIMEOUT
TIMEOUT = 3
dist_source("opt.dis")

def config():

 # configuration: number of processes
 n = 150

 # create n process
 ps = createprocs(P,n)
 config_sim_total_units(10)
 config_trace(False)

 # setup the processes
 for p in ps: AllProcesses[p].setup(ps-{p})
