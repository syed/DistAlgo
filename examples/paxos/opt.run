TIMEOUT = 5

dist_source("opt.dis")

def main():
    nproposers = 10
    nacceptors = 25

    acceptors = createprocs(Acceptor, nacceptors, [])
    proposers = createprocs(Proposer, nproposers,
                            [acceptors, nproposers+nacceptors, nacceptors/2])

    config_max_event_timeout(acceptors | proposers, 3)

    startprocs(acceptors)
    startprocs(proposers)
